<div class="container">
    <h1>order#edit</h1>

    <div class="row">
            <div class="col-12">
                <%= form_with model: [@sender, @order ] do |f| %>
                <div class="form-group">
                    <%= f.label :receiver_name %>
                    <%= f.text_field :receiver_name, autofocus: true, autocomplete: 'receiver_name', class: 'form-control' %>
                </div>

                <div class="form-group">
                    <%= f.label :receiver_phone %>
                    <%= f.text_field :receiver_phone, autocomplete: 'receiver_phone', class: 'form-control' %>
                </div>

                <div class="form-group">
                    <%= f.label :receiver_address %>
                    <%= f.text_area :receiver_address, autocomplete: 'receiver_address', class: 'form-control' %>
                </div>

                <h3>Items</h3>
                <div id='items'>
                    <%= f.fields_for :items do | item | %>
                    <%= render 'item_fields', :f => item %>
                    <% end %>
                    <div class='links'>
                    <%= link_to_add_association 'add item', f, :items, class: "btn btn-primary btn-sm" %>
                    </div>
                </div>

                
                <div class="actions">
                    <%= f.submit("Create" , class: "btn btn-outline-success top-margin" )%>
                </div>
                <% end %>

            </div>
        </div> 





    <%
=begin%>
 <%= semantic_form_for [@sender, @order ] do |f| %>
    <% f.inputs do %>
        <%= f.input :receiver_name %>
        <%= f.input :receiver_phone %>
        <%= f.input :receiver_address %>
        <h3>Items</h3>
        <div id='items'>
        <%= f.semantic_fields_for :items do |item| %>
            <%= render 'item_fields', :f => item %>
        <% end %>
        <div class='links'>
            <%= link_to_add_association 'add item', f, :items %>
        </div>
        </div>
        <% f.buttons do %>
        <%= f.submit 'Save' %>
        <% end %>
    <% end %>
    <% end %> 
<%
=end%>

</div>